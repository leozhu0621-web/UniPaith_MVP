# Local development — for running tests only
# Production and staging always use GPU_MODE=aws

ENVIRONMENT=development
DEBUG=true
DATABASE_URL=postgresql+asyncpg://unipaith:unipaith@localhost:5432/unipaith

# AI Engine — tests need mock mode, real dev should use GPU_MODE=aws
GPU_MODE=mock
AI_MOCK_MODE=true
SCHEDULER_ENABLED=false

# Auth — dev bypass
COGNITO_BYPASS=true
